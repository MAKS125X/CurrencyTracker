<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CurrencyTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.CurrencyTracker" parent="Base.Theme.CurrencyTracker" />
</resources>